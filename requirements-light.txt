requests>=2.28.0
# That's it — the CLI (generate/parallel/full/synthesis/memory) never imports
# flask or web_server.py at module level, so this is all it needs.
#
# Not included (add only if you want them):
#   flask>=3.0.0          — required for `prompt_expert_enhance.py web` (the web UI).
#                            Omitting it is safe: web_server.py detects it's missing
#                            and exits with a clear message instead of crashing.
#   cryptography>=41.0.0  — required only if you enable settings.encrypt_memory.
#                            Without it, memory/sessions.json is stored in plaintext
#                            and the tool warns once instead of failing.
#
# Install: pip install -r requirements-light.txt
