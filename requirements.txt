requests>=2.28.0
flask>=3.0.0            # only needed for the web UI (`prompt_expert_enhance.py web`) — see requirements-light.txt for a CLI-only install
cryptography>=41.0.0    # optional: only needed for settings.encrypt_memory (at-rest encryption of memory/sessions.json)
