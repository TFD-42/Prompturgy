================================================================================
FILE: implementation_order.txt
ROLE: Sequenced build plan — exact order to implement the pre-processor feature
VERSION TARGET: v2.3.0
================================================================================

PHASE 1 — Constants & template  (no logic risk, safe to do first)
------------------------------------------------------------------
1.1  Add PRE_PROCESSOR_TIMEOUT = 30 near other constants at top of file
1.2  Add PRE_PROCESSOR_MAX_TOKENS = 600 near other constants
1.3  Add PRE_PROCESSOR_PROMPT = """...""" as a module-level constant
     Source: pre_processor_prompt_template.txt — copy the template body
     Place: after SYNTH_PROMPT constant, before TECHNIQUES_DB or wherever
     prompts/templates are grouped

Syntax check after phase 1:
    python3 -m py_compile prompt_expert_enhance.py


PHASE 2 — Function  (self-contained, no side effects)
------------------------------------------------------
2.1  Add pre_process_input() function
     Source: pre_processor_function.txt
     Place: AFTER sanitize_input(), BEFORE collect_input()
     (~line 1750 currently)

2.2  Verify function is reachable:
     python3 -c "from prompt_expert_enhance import pre_process_input; print('OK')"

Syntax check after phase 2:
    python3 -m py_compile prompt_expert_enhance.py


PHASE 3 — Settings  (backwards-compatible, defaults safe)
----------------------------------------------------------
3.1  In load_settings() → add to defaults dict:
         "use_pre_processor": True,
         "pre_processor_model": "",

3.2  In load_settings() → add type coercion after existing coercions:
         defaults["use_pre_processor"] = bool(defaults.get("use_pre_processor", True))
         pp_m = defaults.get("pre_processor_model", "")
         defaults["pre_processor_model"] = sanitize_input(str(pp_m).strip(), "model") if pp_m else ""

No existing behavior changes — new keys have safe defaults.

Syntax check after phase 3:
    python3 -m py_compile prompt_expert_enhance.py


PHASE 4 — collect_input() STEP 5  (core UX change)
----------------------------------------------------
4.1  Add STEP 5 block inside collect_input()
     Source: collect_input_patch.txt
     Position: AFTER pick_model_interactive call, BEFORE return statement
     Guard: if settings.get("use_pre_processor", True):

4.2  Verify guard: settings["use_pre_processor"] = False → STEP 5 does not appear
4.3  Verify fallback: if pre_process_input returns raw_input, user still sees it correctly

Syntax check after phase 4:
    python3 -m py_compile prompt_expert_enhance.py

Manual smoke test (interactive):
    python3 prompt_expert_enhance.py
    → menu option 1
    → enter messy French input
    → verify STEP 5 appears and streams
    → accept and confirm generation proceeds


PHASE 5 — menu_advanced() settings UI
--------------------------------------
5.1  Add use_pre_processor toggle in menu_advanced()
5.2  Add pre_processor_model input in menu_advanced() (conditional on use_pre_processor)
     Source: settings_additions.txt → section "MENU 8"

Syntax check after phase 5:
    python3 -m py_compile prompt_expert_enhance.py

Manual test:
    menu 8 → toggle pre-processor OFF → return to menu → verify banner shows "OFF"


PHASE 6 — print_banner() update
---------------------------------
6.1  Add Pre-process line in print_banner()
     Source: banner_patch.txt
6.2  Bump version string: v2.2 → v2.3

Syntax check after phase 6:
    python3 -m py_compile prompt_expert_enhance.py


PHASE 7 — CLI support
-----------------------
7.1  Add --no-preprocess flag in common_args()
     Source: settings_additions.txt → section "CLI"
7.2  In main(): read args.no_preprocess, pass to settings before generation
7.3  In main(): call pre_process_input() on args.task before generation calls
     Source: menu_integration_patch.txt → "CLI PATH NOTES"

Syntax check after phase 7:
    python3 -m py_compile prompt_expert_enhance.py

CLI test:
    python3 prompt_expert_enhance.py generate "messy input" --model llama3:latest
    → verify pre-processor line appears before generation
    python3 prompt_expert_enhance.py generate "messy input" --no-preprocess
    → verify pre-processor is skipped


PHASE 8 — Privacy scan + commit
---------------------------------
8.1  Run privacy scan (must be 10/10 CLEAN):
     python3 tools/privacy_scan.py

8.2  Run full syntax check one final time:
     python3 -m py_compile prompt_expert_enhance.py

8.3  Run all test cases from test_cases.txt manually

8.4  Commit:
     git add prompt_expert_enhance.py
     git commit -m "feat: add pre-processor step — Ollama restructures raw input before full pipeline (v2.3)"

8.5  Push:
     git push origin main


TOTAL ESTIMATED EDITS:
-----------------------
- 3 new constants at top of file
- 1 new function (~50 lines)
- 4 lines in load_settings()
- ~35 lines in collect_input() (STEP 5 block)
- ~10 lines in menu_advanced()
- ~3 lines in print_banner()
- ~5 lines in common_args()
- ~10 lines in main()

Total: ~120 lines added, 0 lines removed from existing logic.
Fully backwards-compatible. All new code is additive or behind a guard.


RISK ASSESSMENT:
----------------
LOW    — pre_process_input() is self-contained, never raises
LOW    — STEP 5 is guarded by use_pre_processor flag
LOW    — settings changes are additive with safe defaults
MEDIUM — collect_input() change touches the main input flow
         → test manually before commit (test cases 1–5 minimum)

================================================================================
